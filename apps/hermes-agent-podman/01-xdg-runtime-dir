#!/bin/sh
# podman fails hard on a missing XDG_RUNTIME_DIR (it lstats, never mkdirs);
# /tmp is a tmpfs/emptyDir at runtime so the dir cannot be baked.
mkdir -p "${XDG_RUNTIME_DIR:-/tmp/xdg-runtime}"
