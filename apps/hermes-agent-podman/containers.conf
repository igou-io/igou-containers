# No SELinux label separation for nested containers: the pod-level user
# namespace + the pod's own SELinux context are the isolation boundary
# (same trust model as podman inside a VM guest).
[containers]
label = false
